# Drift CronJob identity (ADR-017 per-purpose split, ADR-051 naming).
apiVersion: v1
kind: ServiceAccount
metadata:
  name: "{@ service_kebab @}-drift-sa"
  annotations:
    iam.gke.io/gcp-service-account: "{PROJECT_NAME}-drift-staging@{PROJECT_ID}.iam.gserviceaccount.com"
