# =============================================================================
# GCP Dev — ServiceAccount with Workload Identity
# =============================================================================
apiVersion: v1
kind: ServiceAccount
metadata:
  name: "{@ service_kebab @}-sa"
  annotations:
    iam.gke.io/gcp-service-account: "{PROJECT_NAME}-runtime-dev@{PROJECT_ID}.iam.gserviceaccount.com"
