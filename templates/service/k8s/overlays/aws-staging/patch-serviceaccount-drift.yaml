# Drift CronJob identity (ADR-017 per-purpose split, ADR-051 naming).
apiVersion: v1
kind: ServiceAccount
metadata:
  name: "{@ service_kebab @}-drift-sa"
  annotations:
    eks.amazonaws.com/role-arn: "arn:aws:iam::{AWS_ACCOUNT_ID}:role/{PROJECT_NAME}-{@ service_kebab @}-drift-irsa-staging"
