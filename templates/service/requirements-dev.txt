# Development dependencies — tests, linters, load generation. NOT installed in
# the served image, and not needed to train.
#
# Install:
#     pip install -r requirements-dev.txt
#
# Mirrors the `dev` extra in pyproject.toml. The two are kept in lockstep by
# the template repo's `check_dependency_partition.py` gate: ADR-035 declared
# that requirements*.txt is an export of pyproject.toml (invariant I-035-1)
# and nothing enforced it until that gate existed.

-r requirements.txt

pytest               ~= 9.1.1
pytest-cov           ~= 7.1.0
locust               ~= 2.46.5    # tests/load_test.py
httpx                ~= 0.28.1    # FastAPI TestClient transport
jsonschema           ~= 4.26.0    # scripts/validate_quality_gates.py + its test

# ADR-044 replaced black + isort + flake8 with ruff.
ruff                 ~= 0.16.6
mypy                 ~= 2.3.1
bandit               ~= 1.9.4
pre-commit           ~= 4.6.2
